"use client";

/**
 * Organization Bots Page
 *
 * Re-uses the same Bots page component — org context is detected
 * automatically via useAuth.
 *
 * @module app/(dashboard)/organizations/[orgSlug]/bots
 */

export { default } from "@/app/(dashboard)/bots/page";
